# Snake

A classic endless Snake game in a single `index.html` — no build step, no dependencies.

- Works on phone, tablet, and desktop
- Keyboard (arrows / WASD), swipe, or on-screen D-pad; pause button in the header
- Hitting a wall or yourself ends the game
- Best score is saved locally

Open `index.html` in a browser to play.
